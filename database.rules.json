{
  "rules": {
    	"users": {
        	"$uid": {
            ".read": "auth.uid === $uid",
            ".write": "auth.uid === $uid"
          }
      },
      "aliases": {
        ".read": "auth.uid != null",
        ".write": "auth.uid != null"
      }
  }
}